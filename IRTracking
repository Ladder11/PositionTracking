#include <Wire.h>
#include <PVision.h>

PVision ircam;

void setup()
{
  ircam.init();
  Serial.begin(115200);
}

void loop() {
  result = ircam.read();
  if (result&BLOB1) {
    Serial.println(ircam.Blob1.X);
    Serial.println(ircam.Blob1.Y);
  }
}